This basic docker  image is used to build the project. The Dockerfile contains all the necessary instructions for building and running the application in a containerized environment.

### Build Instructions:

docker-compose build
docker-compose up